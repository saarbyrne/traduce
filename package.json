{
  "name": "traduce",
  "version": "2.1.0",
  "private": true,
  "description": "Chrome extension that translates selected text between Spanish and English",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "zip": "node scripts/zip.mjs",
    "typecheck": "tsc -p jsconfig.json"
  },
  "engines": { "node": ">=22" }
}
